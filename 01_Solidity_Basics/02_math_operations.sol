pragma solidity >=0.5.0 <0.6.0; // Specifies the version of Solidity the code is compatible with (between 0.5.0 and 0.6.0).

contract ZombieFactory { // Declares a new contract named 'ZombieFactory'.

    uint dnaDigits = 16; // Declares an unsigned integer variable 'dnaDigits' and initializes it to 16.

}